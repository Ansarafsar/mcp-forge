{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"MCP-Forge","text":"<p>Compile a single YAML spec into a runnable Model Context Protocol server, built on the official <code>mcp</code> SDK.</p> <pre><code># weather.yaml\nname: weather\ntools:\n  - name: get_weather\n    description: Get the current weather for a city.\n    params:\n      - name: city\n        type: str\n    returns: str\n    body: |\n      return f\"Weather in {city}: 22 degrees C, clear skies.\"\n</code></pre> <pre><code>$ mcp-forge build weather.yaml\nOK wrote server.py from weather.yaml\n</code></pre>"},{"location":"#what-you-get","title":"What you get","text":"<ul> <li>One spec \u2192 one server. Tools, resources, and prompts from declarative YAML.</li> <li>Type-safe output. Friendly YAML types become Python annotations; the SDK derives   JSON schemas from them.</li> <li>Offline testing. A built-in mock MCP client runs snapshot tests with no LLM.</li> <li>Hardening built in. Opt into auth, structured logging, and rate limiting per spec.</li> <li>Ship anywhere. Generate a single file or a Docker build context.</li> </ul>"},{"location":"#next-steps","title":"Next steps","text":"<ul> <li>Quickstart \u2014 install and build your first server.</li> <li>Spec reference \u2014 every field explained.</li> <li>CLI reference \u2014 all commands and flags.</li> <li>Testing \u2014 snapshot tests with the mock client.</li> <li>Hardening \u2014 auth, logging, and rate limits.</li> </ul>"},{"location":"cli/","title":"CLI reference","text":"Command What it does <code>mcp-forge init &lt;name&gt; [-t TEMPLATE]</code> Scaffold a spec and a snapshot-test file. <code>mcp-forge new &lt;name&gt; [-t TEMPLATE]</code> Scaffold a spec from a starter template. <code>mcp-forge build &lt;spec&gt; [-o OUT] [--target python\\|docker]</code> Compile a spec. <code>mcp-forge run &lt;spec&gt;</code> Build to a temp file and run the server. <code>mcp-forge dev &lt;spec&gt;</code> Run with hot reload on spec change. <code>mcp-forge test [PATH]</code> Run snapshot tests (<code>*.test.yaml</code>). <code>mcp-forge validate &lt;spec&gt;</code> Validate a spec without generating code. <code>mcp-forge templates</code> List bundled starter templates. <code>mcp-forge version</code> Print the installed version."},{"location":"cli/#build","title":"build","text":"<pre><code>mcp-forge build weather.yaml                 # -&gt; server.py\nmcp-forge build weather.yaml -o out/srv.py   # custom path\nmcp-forge build weather.yaml --no-check      # skip byte-compile\nmcp-forge build weather.yaml --target docker # -&gt; &lt;name&gt;-docker/ build context\n</code></pre> <p>The Docker target emits <code>server.py</code>, <code>requirements.txt</code>, a <code>Dockerfile</code>, and a <code>.dockerignore</code>:</p> <pre><code>mcp-forge build weather.yaml --target docker -o weather-docker\ndocker build -t weather weather-docker &amp;&amp; docker run -i weather\n</code></pre>"},{"location":"cli/#dev","title":"dev","text":"<p><code>dev</code> watches the spec file and rebuilds + restarts the server whenever it changes. Tune the poll interval with <code>--interval</code> (seconds).</p> <p>Run <code>mcp-forge --help</code> or <code>mcp-forge &lt;command&gt; --help</code> for full details.</p>"},{"location":"hardening/","title":"Hardening","text":"<p>Opt into production-readiness signals per spec. When enabled, MCP-Forge inlines the needed helpers into the generated server \u2014 it stays a single, dependency-light file.</p>"},{"location":"hardening/#auth","title":"Auth","text":"<pre><code>auth:\n  type: api_key        # api_key | bearer_token | oauth_stub\n  env: MCP_API_KEY     # env var that must hold the expected secret\n  header: X-API-Key    # informational\n</code></pre> <p>Every tool call runs an auth guard first. If the configured environment variable is not set, the call is rejected with a <code>PermissionError</code>. <code>oauth_stub</code> generates the same gate plus a marked place to add real token verification.</p>"},{"location":"hardening/#structured-logging","title":"Structured logging","text":"<pre><code>observability:\n  logging: true\n</code></pre> <p>Each tool call is logged to stderr as one JSON line, with the tool name, arguments, duration in milliseconds, and success/error:</p> <pre><code>{\"tool\":\"get_weather\",\"args\":{\"city\":\"London\"},\"ms\":0.42,\"ok\":true}\n</code></pre> <p>stdout stays reserved for the MCP protocol, so logging never corrupts the channel.</p>"},{"location":"hardening/#forward-logs-to-an-audit-endpoint","title":"Forward logs to an audit endpoint","text":"<pre><code>observability:\n  logging: true\n  audit_url: http://localhost:8080/ingest\n</code></pre> <p>Each log line is also POSTed to <code>audit_url</code> (best-effort; failures never break a tool call). This is the integration point for an external audit service.</p>"},{"location":"hardening/#rate-limiting","title":"Rate limiting","text":"<p>A thread-safe token bucket per tool:</p> <pre><code>observability:\n  rate_limit:\n    default: { rate: 30, per: 60 }     # 30 calls / 60s for every tool\n    per_tool:\n      get_weather: { rate: 10, per: 60 } # override for one tool\n</code></pre> <p>When a tool exceeds its budget the call raises <code>RuntimeError: rate limit exceeded</code>.</p>"},{"location":"hardening/#putting-it-together","title":"Putting it together","text":"<p>See <code>examples/secure-weather.yaml</code> for a spec that combines auth, logging, and rate limiting.</p>"},{"location":"quickstart/","title":"Quickstart","text":""},{"location":"quickstart/#install","title":"Install","text":"<p>MCP-Forge needs Python 3.10+.</p> <pre><code>git clone https://github.com/Ansarafsar/mcp-forge.git\ncd mcp-forge\npip install -e \".[serve]\"\n</code></pre> <p>The <code>serve</code> extra installs the <code>mcp</code> SDK, needed to run a generated server. The compiler itself has no MCP dependency.</p>"},{"location":"quickstart/#your-first-server-in-60-seconds","title":"Your first server in 60 seconds","text":"<pre><code># 1. Scaffold a spec (and a matching test file)\nmcp-forge init my-weather --template weather\n\n# 2. Compile it\nmcp-forge build my-weather.yaml -o server.py\n\n# 3. Run it (speaks MCP over stdio)\npython server.py\n</code></pre> <p>Or skip straight to a live, hot-reloading dev loop:</p> <pre><code>mcp-forge dev my-weather.yaml\n</code></pre>"},{"location":"quickstart/#use-it-from-claude-desktop","title":"Use it from Claude Desktop","text":"<p>Add the generated server to <code>claude_desktop_config.json</code>:</p> <pre><code>{\n  \"mcpServers\": {\n    \"weather\": {\n      \"command\": \"python\",\n      \"args\": [\"/absolute/path/to/server.py\"]\n    }\n  }\n}\n</code></pre> <p>Restart Claude Desktop; the tools appear in the client.</p>"},{"location":"spec-reference/","title":"Spec reference","text":"<p>A spec is one YAML file describing one server.</p>"},{"location":"spec-reference/#top-level","title":"Top level","text":"Field Type Required Description <code>name</code> string yes Server name (shown to MCP clients). <code>version</code> string no Defaults to <code>0.1.0</code>. <code>description</code> string no Free-text summary, used in the file header. <code>mcp_version</code> string no Protocol version to pin in the output. <code>prelude</code> string no Raw Python inserted at module level (imports, shared state). <code>auth</code> mapping no See Hardening. <code>observability</code> mapping no See Hardening. <code>tools</code> list * MCP tools. <code>resources</code> list * MCP resources. <code>prompts</code> list * MCP prompts. <p>* At least one of <code>tools</code>, <code>resources</code>, or <code>prompts</code> must be present.</p>"},{"location":"spec-reference/#tools","title":"Tools","text":"<pre><code>tools:\n  - name: get_weather          # valid Python identifier\n    description: Get weather.   # becomes the tool docstring\n    params: [ ... ]            # see Parameters\n    returns: str               # return annotation (default: str)\n    body: |                    # inline Python; omit for a NotImplementedError stub\n      return \"...\"\n</code></pre>"},{"location":"spec-reference/#resources","title":"Resources","text":"<pre><code>resources:\n  - name: list_notes\n    uri: notes://all           # the URI clients address\n    description: All notes.\n    returns: str\n    body: |\n      return \"...\"\n</code></pre>"},{"location":"spec-reference/#prompts","title":"Prompts","text":"<pre><code>prompts:\n  - name: summarize\n    description: Summarize text.\n    params: [ ... ]\n    template: |                # rendered as an f-string; {param} interpolates\n      Summarize this: {text}\n</code></pre>"},{"location":"spec-reference/#parameters","title":"Parameters","text":"<pre><code>params:\n  - name: city          # valid Python identifier\n    type: str           # see types below\n    description: ...     # optional\n    default: celsius    # optional; makes the param optional with this default\n    optional: true      # optional; with no default, becomes Optional[T] = None\n    choices: [a, b]     # required when type is `enum`\n</code></pre> <p>Types: <code>str</code>, <code>int</code>, <code>float</code>, <code>bool</code>, <code>list</code>, <code>dict</code>, <code>any</code>, and <code>enum</code> (with <code>choices</code>). An <code>enum</code> becomes a <code>typing.Literal[...]</code>.</p>"},{"location":"spec-reference/#shared-state-with-prelude","title":"Shared state with <code>prelude</code>","text":"<p><code>prelude</code> is raw Python inserted at module level \u2014 useful for imports, helpers, or state shared across tools:</p> <pre><code>prelude: |\n  _NOTES: dict[str, dict[str, str]] = {}\n</code></pre>"},{"location":"testing/","title":"Testing","text":"<p>MCP-Forge ships a mock MCP client and a snapshot test harness, so you can verify a generated server without an LLM or any real client.</p>"},{"location":"testing/#how-it-works","title":"How it works","text":"<p><code>mcp-forge test</code> finds <code>*.test.yaml</code> files, builds the referenced spec to a temporary server, launches it as a subprocess, and drives it with <code>MockMCPClient</code> \u2014 a dependency-free client that speaks MCP's newline-delimited JSON-RPC over stdio. Each case's result is compared to its expectation.</p>"},{"location":"testing/#test-file-format","title":"Test file format","text":"<p>A <code>*.test.yaml</code> sits next to its spec:</p> <pre><code>spec: weather.yaml          # defaults to the same name without `.test`\nenv:                        # optional env vars for the server process\n  MCP_API_KEY: secret\ncases:\n  - name: celsius           # optional label\n    tool: get_weather       # call a tool...\n    args: { city: London, units: celsius }\n    expect: \"Weather in London: 22 degrees C, clear skies.\"\n\n  - resource: notes://all   # ...or read a resource\n    expect: \"(no notes yet)\"\n\n  - prompt: summarize_note   # ...or render a prompt\n    args: { body: hello }\n    expect_contains: \"Summarize\"\n</code></pre>"},{"location":"testing/#matchers","title":"Matchers","text":"Key Meaning <code>expect</code> Exact string match (whitespace-trimmed). <code>expect_contains</code> Substring must appear in the result. <code>expect_error</code> The call is expected to raise (e.g. auth or rate-limit rejection)."},{"location":"testing/#running","title":"Running","text":"<pre><code>mcp-forge test examples/            # all *.test.yaml under a directory\nmcp-forge test examples/weather.test.yaml\n</code></pre> <p>The command exits non-zero on any failure and prints a colored diff pointing at the exact line that changed \u2014 wire it straight into CI.</p>"}]}