"""{{ spec.name }} — generated by MCP-Forge.

{{ spec.description or "An MCP server." }}

Do not edit by hand: regenerate with `mcp-forge build <spec>.yaml`.
{% if spec.mcp_version %}MCP protocol version: {{ spec.mcp_version }}{% endif %}
"""

from __future__ import annotations
{% if typing_imports %}
from typing import {{ typing_imports | join(", ") }}
{% endif %}
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("{{ spec.name }}")

{% if forge_runtime %}
{{ forge_runtime }}
{% endif %}
{% if prelude %}
{{ prelude }}

{% endif %}
{% for tool in tools %}
{{ tool }}

{% endfor %}
{% for resource in resources %}
{{ resource }}

{% endfor %}
{% for prompt in prompts %}
{{ prompt }}

{% endfor %}
if __name__ == "__main__":
    mcp.run()
